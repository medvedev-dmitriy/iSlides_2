;(function(){
    'use strict';

    angular.module('iSlidesApp')
        .factory('ShowPresentationService', ShowPresentationService);

    ShowPresentationService.$inject = [
        '$location'
    ];

    function ShowPresentationService($location){

    }
})();